{
  "name": "@andreadev/custom-event-target",
  "version": "1.0.2",
  "description": "A simple yet effective event system to avoid reinventing the wheel every time",
  "main": "dist/CustomEventTarget.js",
  "types": "dist/CustomEventTarget.d.ts",
  "scripts": {
    "build": "tsc --target esnext --outDir ./dist --declaration src/*.ts",
    "test": "node --import tsx --test **/*.test.ts"
  },
  "type": "module",
  "author": "Andrea Dragotta",
  "license": "ISC",
  "devDependencies": {
    "tsx": "^3.14.0"
  }
}
